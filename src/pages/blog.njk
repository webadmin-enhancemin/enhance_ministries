---
layout: layouts/base.njk
title: Blog
navStyle: solid
description: "Insights on pastoral health, ministry leadership, and faith from Matt Swigart and Enhance Ministries."
keywords: "ministry blog, pastoral coaching, Matt Swigart, faith leadership, pastor care resources"
canonical: "/blog/"
hasDropdown: true
permalink: /blog/
---

<!-- Hero Section -->
<header class="coaching-hero">
    <div class="container">
        <span class="event-badge">Latest Insights</span>
        <h1>Blog</h1>
        <p class="coaching-hero-text">Encouragement, insights, and resources for ministry leaders.</p>
    </div>
</header>

<!-- Blog Posts Grid -->
<section class="section">
    <div class="container">
        {% set posts = collections.blog | reverse %}
        {% if posts and posts.length %}
        <div class="blog-grid">
            {% for post in posts %}
            <article class="blog-card">
                <div class="blog-card-content">
                    {% if post.data.category %}
                    <span class="event-badge blog-card-category">{{ post.data.category }}</span>
                    {% endif %}
                    <h2 class="blog-card-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
                    <p class="blog-card-meta">{{ post.data.date | readableDate }}{% if post.data.author %} &middot; {{ post.data.author }}{% endif %}</p>
                    <p class="blog-card-excerpt">{{ post.data.description }}</p>
                    <a href="{{ post.url }}" class="btn btn-primary btn-sm">Read More</a>
                </div>
            </article>
            {% endfor %}
        </div>
        {% else %}
        <p class="blog-empty">No posts yet. Check back soon!</p>
        {% endif %}
    </div>
</section>

<!-- Subscribe / RSS -->
<section class="section section-alt">
    <div class="container">
        <div class="section-header">
            <h2>Stay Connected</h2>
            <p class="section-subtitle">Receive encouragement and insights for ministry leaders.</p>
        </div>
        <div class="blog-listing-cta">
            <a href="/contact/" class="btn btn-primary btn-lg">Get in Touch</a>
            <a href="/feed.xml" class="btn btn-outline btn-lg">RSS Feed</a>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="section section-cta">
    <div class="container">
        <div class="cta-content">
            <h2>Ready to Take the Next Step?</h2>
            <p>Whether you're looking for coaching, consulting, or simply need someone in your corner — Enhance Ministries is here.</p>
            <a href="/contact/" class="btn btn-outline-white btn-lg">Connect With Us</a>
        </div>
    </div>
</section>
